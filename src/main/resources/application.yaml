spring:
  application:
    name: inventory
  # email 전송
  mail:
    host: smtp.gmail.com
    port: 587
    username: ${MAIL_USERNAME:}
    password: ${MAIL_PASSWORD:}
    default-encoding: UTF-8
    properties:
      "[mail.smtp.auth]": true
      "[mail.smtp.starttls.enable]": true
      "[mail.smtp.connectiontimeout]": 5000
      "[mail.smtp.timeout]": 5000
      "[mail.smtp.writetimeout]": 5000

  config:
    import: optional:file:.env[.properties]

  # 데이터베이스 연결 설정
  datasource:
    url: ${DB_URL}
    driver-class-name: ${DB_DRIVER}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}

  # Hibernate 설정
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
    properties:
      hibernate:
        format_sql: true
    open-in-view: false

  # Redis Connection Configuration
  data:
    redis:
      host: ${REDIS_HOST}
      port: ${REDIS_PORT:6379}
      password: ${REDIS_PASSWORD}
      database: ${REDIS_DATABASE:0}

  # RabbitMQ Connection Configuration
  rabbitmq:
    host: ${RABBITMQ_HOST}
    port: ${RABBITMQ_PORT}
    username: ${RABBITMQ_USERNAME}
    password: ${RABBITMQ_PASSWORD}

  # 스케줄러 스레드 풀. 기본값 1이면 텔레그램 롱폴링이 유통기한 알림 스케줄러를 막는다.
  task:
    scheduling:
      pool:
        size: 2

  ai:
    google:
      genai:
        api-key: ${GOOGLE_API_KEY:dummy}
        chat:
          model: gemini-3.1-flash-lite

server:
  port: ${SERVER_PORT:10411}

management:
  endpoints:
    web:
      exposure:
        include: health, service-registry, prometheus
  endpoint:
    health:
      show-details: never
  health:
    mail:
      enabled: false # 메일이 안된다고 서버가 못뜨면 안되기 때문에 메일 헬스체크 비활성화

# 단톡방 챗봇 (텔레그램)
chatbot:
  telegram:
    enabled: ${TELEGRAM_CHATBOT_ENABLED:false}
    bot-token: ${TELEGRAM_BOT_TOKEN:}
    api-base-url: https://api.telegram.org
    poll-timeout-seconds: 30
    command-prefix: "/chatbot"

# medicine API
medicine:
  api:
    base-url: https://apis.data.go.kr/1471000/DrugPrdtPrmsnInfoService07
    detail-path: /getDrugPrdtPrmsnDtlInq06
    service-key: d9149176405bff9304536370ec6a5d7278891b544707a426bfb79fac8f87e586

security:
  jwt:
    access-token-ttl: 30m
    allowed-algorithms:
      - RS256
    issuer: account-api
    key-id: account-key-001
    audiences:
      - inventory-api

